StrBlob b1;
{
	StrBlob b2={"2","an","the"};
	b1=b2;
	b2.push_back("about");
}
//因为b2的生命周期在代码块之内，所以，b2在代码结尾处已被销毁，但b2的内部元素并没有被销毁，
//所以在代码的结尾处b2包含4个元素
//因为b1和b2公用同一个vector，且b1的生命周期不在代码块之内，所以，在代码的结尾处b1包含4个元素

将entry和nums定义为const auto&的原因时为了更好的效率，原因如下：
1) 定义为引用而不是普通的数据成员，是为了节省空间（要是不定义成引用，每次for循环都会生成对应的一个临时变量，该变量的构造和析构都是一笔性能开销）
2) 定义为auto类型，是为了写代码方便些，毕竟c++11的新特性
3) 定义为常量类型，是因为for循环内部并没有改变该变量的值的需求，这样定义可以发现未知的误操作

a) 选用list，为了保持有序，需要在容器中间插入元素，而在这点上，vector和deque
             都比较耗时，所以选用list
b) 选用deque，如果程序需要在头尾位置插入或删除元素，但不会在中间位置进行
              插入或删除操作，则使用deque(教程原话)
c) 选用list，为了保持有序，需要在容器中间插入元素，而在这点上，vector和deque
             都比较耗时，所以选用list

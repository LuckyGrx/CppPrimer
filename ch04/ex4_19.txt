a) ptr!=0&&*ptr++
说明：判断ptr是否为空，要是不空的话，判断指针所指向的对象是否为真，
      并进行自增运算
b) ival++&&ival
说明：判断ival和ival+1的值是否为真
c) vec[ival++]<=vec[ival]
说明：该表达式不正确，因为<=运算符左端修改了ival的值，但右端又用到了ival的值，
      所以在不同的编译器条件下，结果是不同的，即该结果未定义
修改为：vec[ival]<=vec[ival+1]

someValue ? ++x, ++y : --x, --y
//由于条件运算符的优先级大于逗号运算符，所以该表达式等价于 (someValue ? (++x, ++y) : --x), --y
//首先判断someValue的值，要是为真的话，进行++x和++y操作，然后表达式的结果为--y
                         要是为假的话，进行--y操作，然后表达式的结果为--y

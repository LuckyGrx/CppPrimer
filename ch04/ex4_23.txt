string s = "word";
string p1 = s + s[s.size() - 1] == 's' ? "" : "s" ;
//因为在第二个表达式中，优先级大小为：下标运算符([]) > 加法运算符(+) > 相等运算符(==) > 条件运算符(?:) > 赋值运算符(=)
//所以，该表达式的等价表示为 string p1 = ((s + s[s.size() -1]) == 's') ? "" : "s" ;
//显然这样是不符合需求的，可以更改为：
//string p1 = s + ( s[s.size() -1] == 's' ? "" : "s");
